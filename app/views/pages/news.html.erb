<% @news.each do |news| %>
<%= image_tag news.image if news.image.attached? %>
  <h2><%= news.title %></h2>
  <p><%= news.content %></p>

  <h3>Comentarios</h3>
  <% news.comments.each do |comment| %>
    <p><%= comment.content %></p>
  <% end %>

  <hr>
<% end %>